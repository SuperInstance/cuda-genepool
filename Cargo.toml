[package]
name = "cuda-genepool"
version = "0.1.0"
edition = "2021"
description = "Gene pool — shared pattern library for agent evolution and code sharing"

[dependencies]
cuda-equipment = { path = "../cuda-equipment" }